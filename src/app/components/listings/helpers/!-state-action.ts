// import { getPendingAction, setPendingAction } from "./!-get-set-pending";

// // Global state for pending actions
// // let pendingAction = null;
// setPendingAction(null);

// function isTaskActionPending(action, taskId) {
//   const pendingAction = getPendingAction();
//   return pendingAction?.id === taskId && pendingAction.type === action;
// }

// function isTaskBusy(taskId) {
//   const pendingAction = getPendingAction();
//   return pendingAction?.id === taskId;
// }

// export { isTaskActionPending, isTaskBusy };
